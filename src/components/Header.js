export default function Header() {
  return (
    <header className="w-full max-w-[75em] mb-8 card-animate">
      <div className="flex-1">
        <h1 className="text-3xl sm:text-4xl lg:text-5xl font-bold mb-2">Spacebar Explorer</h1>
        <p className="text-sm sm:text-base text-[#cfcfe0] mt-2 leading-relaxed">
          Explore Spacebar-compatible clients, instances, and guilds. Browse, review, and discover the best options for your needs.
        </p>
      </div>
    </header>
  );
}
